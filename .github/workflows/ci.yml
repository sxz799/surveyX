name: CI

on:
  push:
    branches: [main]

jobs:
  build-web:
    strategy:
      matrix:
        node-version: [16.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        
    - name: Build Web
      run: |
        sed -i '9s|//||' vite.config.js
        npm install && npm run build
      working-directory: ./web
      
    - name: Move Files
      run: |
        mkdir surveyX_WEB
        tar -czf surveyX-artifact-web.tar.gz web/dist
        mv surveyX-artifact-web.tar.gz surveyX_WEB/
      
    - name: Upload Artifact-Web
      uses: actions/upload-artifact@v2
      with:
       name: surveyX-artifact-web
       path: ${{ github.workspace }}/surveyX_WEB

  build-linux:
    needs: build-web
    runs-on: ubuntu-latest
    container: docker.io/centos:7
    steps:
    - uses: actions/checkout@v3

    - name: intall deps
      run: |
        yum install -y wget tar gcc automake autoconf libtool make

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: 1.21

    - name: Build Server for Linux
      run: go build -ldflags="-s -w" -o bin/app_linux .
      working-directory: ./server

    - name: Download Web Artifact
      uses: actions/download-artifact@v2.1.1
      with:
       name: surveyX-artifact-web

    - name: Copy Files
      run: |
        mkdir surveyX_Linux
        tar xzf surveyX-artifact-web.tar.gz -C .
        cp -r web/dist surveyX_Linux/
        cp -r server/conf.yaml surveyX_Linux/
        cp -r server/bin/app_linux surveyX_Linux/

    - name: Upload Artifact-Linux
      uses: actions/upload-artifact@v2
      with:
       name: surveyX-artifact-linux
       path: ${{ github.workspace }}/surveyX_Linux

    - name: Download a Build Artifact-Linux
      uses: actions/download-artifact@v2.1.1
      with:
       name: surveyX-artifact-linux

  build-mac:
    needs: build-web
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: 1.21

    - name: Build Server for Mac
      run: go build -ldflags="-s -w" -o bin/app_mac .
      working-directory: ./server

    - name: Download Web Artifact
      uses: actions/download-artifact@v2.1.1
      with:
       name: surveyX-artifact-web

    - name: Copy Files
      run: |
        mkdir surveyX_Mac
        tar xzf surveyX-artifact-web.tar.gz -C .
        cp -r web/dist surveyX_Mac/
        cp -r server/conf.yaml surveyX_Mac/
        cp -r server/bin/app_mac surveyX_Mac/

    - name: Upload Artifact-Mac
      uses: actions/upload-artifact@v2
      with:
       name: surveyX-artifact-mac
       path: ${{ github.workspace }}/surveyX_Mac

    - name: Download a Build Artifact-Mac
      uses: actions/download-artifact@v2.1.1
      with:
       name: surveyX-artifact-mac

  build-windows:
    needs: build-web
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: 1.21

    - name: Build Server for Windows
      run: go build -ldflags="-s -w" -o bin/app_windows.exe .
      working-directory: ./server

    - name: Download Web Artifact
      uses: actions/download-artifact@v2.1.1
      with:
       name: surveyX-artifact-web

    - name: Copy Files
      run: |
        mkdir surveyX_Windows
        tar xzf surveyX-artifact-web.tar.gz -C .
        cp -r web/dist surveyX_Windows/
        cp -r server/conf.yaml surveyX_Windows/
        cp -r server/bin/app_windows.exe surveyX_Windows/

    - name: Upload Artifact-Windows
      uses: actions/upload-artifact@v2
      with:
       name: surveyX-artifact-windows
       path: ${{ github.workspace }}/surveyX_Windows

    - name: Download a Build Artifact-Windows
      uses: actions/download-artifact@v2.1.1
      with:
       name: surveyX-artifact-windows
