db :
  sqlType : sqlite  # mysql || sqlite || postgres
  sqliteUrl: survey.db
  postgresUrl : postgres://default:s8kKoq3BfrFv@ep-bold-bird-66330765-pooler.ap-southeast-1.postgres.vercel-storage.com:5432/verceldb  #
  mysqlUrl :  # username:password@tcp(host:port)/database?charset=utf8mb4&parseTime=True&loc=Local
server :
  port : 3000
  ginMode : release # debug || release || test
admin :
  key : 123456

